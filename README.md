# housingPanda-SWEProject
The project is built using React and Node.js, with Firebase as the backend database.
The homepage is divided into two sections: **Admin** and **User**.

* **Admin** has the authority to view and manage data fetched from the database.
* **User** is allowed to submit data through the interface.
